node_modules/
dist/
*.log
.DS_Store
.env
.env.local
# Local Claude Code MCP config — user-specific (env vars, paths).
.mcp.json

# Local launch prep materials (marketing copy, response templates, timelines).
# Not part of the shipped product.
launch/
